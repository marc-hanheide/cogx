;; generated by MAPL2PDDL
(define (problem cosytask) 
(:domain explorer)

(:objects
	R2D2 - robot
	PatrickOffID ChandanaOffID corrID the_kitchen - area
	coffee cookies mug - movable
	kitchen_door - door
)

(:init
	(doorstate open_space : open)

	(connects open_space PatrickOffID corrID)
	(connects open_space corrID PatrickOffID )

	(connects open_space ChandanaOffID corrID)
	(connects open_space corrID ChandanaOffID )

	(connects kitchen_door corrID the_kitchen)
	(connects kitchen_door the_kitchen corrID)
	(doorstate kitchen_door : open)	

	(pos cookies : the_kitchen)
	(domain (pos coffee) : (PatrickOffID ChandanaOffID the_kitchen) )
	(domain (pos mug) : (PatrickOffID ChandanaOffID the_kitchen) )
	(domain (area_category the_kitchen) : (PatrickOffName corridor kitchen) )

	;; a *false* assumption	
	(area_category ChandanaOffID : kitchen)

	(pos r2d2    : PatrickOffID)
)

(:goal (and
	(exists (?ar - area) (and 
		(area_category ?ar : kitchen)
		(pos mug : ?ar)
	))
)

))
