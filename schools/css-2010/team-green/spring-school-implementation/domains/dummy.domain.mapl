(define (domain cogx)
        (:requirements :mapl :adl :object-fluents)
	(:predicates
                (success)
	)
	       
        (:action succeed
	         :agent (?a - agent)
	         :effect (success)
        )
)