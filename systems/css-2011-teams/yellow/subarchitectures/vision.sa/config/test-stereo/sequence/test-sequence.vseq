#
# The file contains multiple sections that are processed in the
# order they are defined. Each section defines either global
# variables or a video sequence. The section name represetns a
# unique sequence identifier. Global variables are stored in
# the section called "[:global:]". There can be multiple
# global sections.
#
# The parameters of a sequence are defined with the "sequence=" variable.
# The parameters are in the format accepted by OpenCvImgSeqServer.
# The variables "in" and "out" may some day be used to define transitions
# between sequences. 
#
# The special variable INI_DIR contains the directory where the video
# sequence confiuguration file is stored and can be used to define sequences
# with images that are stored in a path relative to the config file's path.


[:global:]
# Image paths will be relative to this file
imagepath=%(INI_DIR)

[MessDarjeeling]
dir=%(imagepath)
files=%(dir)/image%03d-L.png.jpg  %(dir)/image%03d-R.png.jpg
in=--files "%(files)" --start 0 --end 10
sequence=--files "%(files)" --start 10 --end 15
out=--files "%(files)" --start 15 --end 23

[Empty]
dir=%(imagepath)
files=%(dir)/image%03d-L.png.jpg  %(dir)/image%03d-R.png.jpg
sequence=--files "%(files)" --start 0 --end 2
 
