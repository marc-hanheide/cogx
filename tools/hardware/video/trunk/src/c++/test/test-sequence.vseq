# Parameters are in the format accepted by OpenCvImgSeqServer.
#
# Each section defines either global variables or a sequence.
# The parameters of a sequence are defined with the "sequence=" variable.
# The variables "in" and "out" may some day be used to define transitions
# between sequences. 
# The special variable INI_DIR contains the directory where this file is
# stored and can be used to define sequences with images that are stored
# in a path relative to this file's path.

[:global:]
imagepath=subarchitectures/vision.sa/config/test-ferns
tea_lr=%(imagepath)/tea%03d.jpg  %(imagepath)/tea%03d.jpg

[TeaStart]
files=%(tea_lr)
sequence=--files "%(files)" --start 0 --end 1 --repeatframe 4

[TeaEnd]
files=%(tea_lr)
in=--files "%(files)" --start 2 --end 2
sequence=--files "%(files)" --start 2 --end 3 --repeatframe 4
out=--files "%(files)" --start 3 --end 3

[TeaAll]
files=%(tea_lr)
in=--files "%(files)" --start 0 --end 0
sequence=--files "%(files)" --start 0 --end 3
out=--files "%(files)" --start 3 --end 3
