
# (2011-12-23, mmarko):
#   Build only the SOIFilter target with dependencies. Improves build time.
# Prerequisites:
#   1. cmake .. was executed in cogxroot/BUILD
#   2. castenv.sh was generated by CAST Control from Cast or Build.Make* menu)
cogxroot=../../../../../../..
all:
	cd $(cogxroot)/BUILD \
	   && if [ -f castenv.sh]; then source castenv.sh; fi \
	   && make SOIFilter
