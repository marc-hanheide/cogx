function mc=pdf2mc(pdf)

mc.mu=pdf.mu;
mc.weights=pdf.weights;
mc.covariances=pdf.covariances;
mc.components=pdf.components;
mc.params.pars=pdf.pars;
mc.params.pars2=pdf.pars2;
mc.params.scale=pdf.scale;
mc.params.nMaxComponents=pdf.nMaxComponents;
mc.params.Curr_covariances=pdf.Curr_covariances;
