PROJECT(MultiSATester)
SET(SOURCES MultiSATester.cpp)
SET(HEADERS MultiSATester.hpp)
ADD_LIBRARY(MultiSATester SHARED ${SOURCES})
TARGET_LINK_LIBRARIES(MultiSATester TestingIDL)
INSTALL(TARGETS MultiSATester LIBRARY DESTINATION lib)
INSTALL(FILES ${HEADERS} DESTINATION include/testing)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${OMNIORB4_LINK_FLAGS} ${OMNIORB4_CFLAGS})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} FrameworkInterface FrameworkIDL FrameworkCore)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} CASTCore CASTArchitecture CASTIDL)