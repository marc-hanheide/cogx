;; generated by MAPL2PDDL
(define (problem cosytask) (:domain explorer)

(:objects
	R2D2 - robot
	patrics_PatrickOffName chandanas_PatrickOffName the_corridor the_kitchen - area
	mug cookies - movable
	kitchen_door - door
)

(:init
	(doorstate open_space : open)

	(area_category patrics_PatrickOffName : PatrickOffName)
	(area_category chandanas_PatrickOffName : PatrickOffName)
	(area_category the_kitchen : kitchen)
	(area_category the_corridor : corridor)
	(connects open_space patrics_PatrickOffName the_corridor)
	(connects open_space the_corridor patrics_PatrickOffName )
	(connects open_space chandanas_PatrickOffName the_corridor)
	(connects open_space the_corridor chandanas_PatrickOffName)

	(connects kitchen_door the_corridor the_kitchen)
	(connects kitchen_door the_kitchen the_corridor)
	(doorstate kitchen_door : open)	

	(pos cookies : the_kitchen)
	(domain (pos mug) : (patrics_PatrickOffName chandanas_PatrickOffName the_kitchen) )
	(domain (area_category the_kitchen) : (PatrickOffName corridor kitchen) )

	(pos r2d2    : patrics_PatrickOffName)
)

(:goal (and
	;; robot does not know where the mug is, but has a map already
	(pos mug : patrics_PatrickOffName)
)

))
