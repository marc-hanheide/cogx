(define (problem task2task) (:domain task2)

(:objects
	loc1 loc2 loc3 loc4 loc5 - place
)

(:init
	   (= (visited loc1) false)
	   (= (visited loc2) false)
	   (= (visited loc3) false)
	   (= (visited loc4) false)
	   (= (visited loc5) false)
	   (= (is_in) loc1)
)

(:goal 
       (forall (?loc - place) (= (visited ?loc) true))
)

)